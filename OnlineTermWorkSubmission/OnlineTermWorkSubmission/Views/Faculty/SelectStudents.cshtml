@model IEnumerable<OnlineTermWorkSubmission.Models.Subject>

@{
   
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>SelectStudents</title>
</head>
<body>
    <p>
        @Html.ActionLink("Back To Students", "ViewStudent", new { id = ViewBag.id })
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.subject_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.semester)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.subject_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.semester)
                </td>
                <td>
                    @Html.ActionLink("View Enrolled Students", "ViewEnrolledStudent", new { sid = item.subject_id, id = ViewBag.id })
                </td>
            </tr>
        }

    </table>
</body>
</html>
