@model OnlineTermWorkSubmission.Models.Faculty

@{
    ViewBag.Title = "index";
}

<div class="jumbotron">
    <h3>FACULTY INDEX</h3>

</div>

<div>
    @Html.ActionLink("view student data", "viewstudent") |

    @Html.ActionLink("log out", "logout") |

    @Html.ActionLink("view subject", "viewsubject", new { id = ViewBag.id })

</div>





